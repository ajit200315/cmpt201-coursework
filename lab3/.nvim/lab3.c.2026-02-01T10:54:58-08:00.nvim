#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
  char *store[5];
  int count = 0;
  int index = 0;

  for(int i = 0 ; i < 5 ; i++)
  {
    store[i] = NULL;
  }

  while(1)
  {
    printf("Enter input: ");
    fflush(stdout);
    char *line = NULL ;
    size_t size = 0;
    ssize_t len = getline(&line,&size,stdin);

    if(len == -1)
    {
      free(line);
      break;
    }

    if(store[index] != NULL)
    {
      free(store[index]);
    }
    store[index] = line; 
    index = (index + 1) % 5 ; 
    if(count < 5)
    {
      count++;
    }
    for(int i = 0 ; i< count ; i++)
    {
      int pos = (start + i)%5; 
      printf("%s",store[pos]);
    }
  }

for(int i = 0 ; i<5 ; i++)
{
  free(store[i]);
}

return 0 ; 
}
